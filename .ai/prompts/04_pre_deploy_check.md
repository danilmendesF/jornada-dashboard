# 💬 PROMPT 04: CHECAGEM PRÉ-DEPLOY (Validação Automatizada)

Copie e envie o prompt abaixo antes de enviar alterações para produção ou staging:

---

```markdown
@IA Atue como Quality-Validator no projeto Jornada Dashboard.

Execute a bateria completa de checagem pré-deploy:
1. Rode a suíte de testes automatizada `node scripts/validate.js`.
2. Rode o atualizador automático do RAG `node scripts/update_state.js`.
3. Verifique se há algum erro de sintaxe ou contrato quebrado.
4. Apresente um resumo executivo com o status de prontidão para deploy.
```
